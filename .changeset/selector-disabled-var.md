---
"@lowdefy/modules-mongodb-workflows": minor
---

The `selector` form component takes a `disabled` var (boolean, default `false`), wired to the block's `disabled` property. With the var unset, behaviour is unchanged.
