---
"@lowdefy/modules-mongodb-layout": minor
---

Add `hide_footer` var to the layout `page` component. When set to `true`, the footer slot is omitted entirely (no styled wrapper rendered). Defaults to `false`, preserving existing behavior.
