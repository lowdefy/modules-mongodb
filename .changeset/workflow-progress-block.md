---
"@lowdefy/modules-mongodb-plugins": minor
"@lowdefy/modules-mongodb-workflows": minor
---

Add the WorkflowProgress block and the workflows module's `workflow-progress` component — a presentation variant of `actions-on-entity` that renders an entity's workflows as collapsible sections of grouped, status-colored action buttons, with progress rings, done-fractions, and the shared check-action click handling baked in.
