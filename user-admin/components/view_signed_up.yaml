id: signed_up
type: Paragraph
visible:
  _eq:
    - _state: user.is_user
    - true
properties:
  content:
    _nunjucks:
      template: |
        Signed up at {{ sign_up_date | date('YYYY-MM-DD HH:mm')}}
      on:
        sign_up_date:
          _state: user.sign_up
