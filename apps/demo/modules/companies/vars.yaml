event_display:
  demo:
    create-company: "{{ user.profile.name }} created {{ target.name }}"
    update-company: "{{ user.profile.name }} updated {{ target.name }}"
# TODO: Slot wiring for activities tile (per design decisions.md §7) is
# blocked by a Lowdefy framework limitation — `_ref { module, component }`
# inside an entry's vars cannot resolve, because parseLowdefyYaml walks
# vars content before modules are registered (resolveDepTarget throws
# "no module with that entry id was registered"). Re-enable once the
# framework supports deferring cross-module refs in app-level vars:
#   components:
#     sidebar_slots:
#       - _ref:
#           module: activities
#           component: tile_activities
#           vars:
#             reference_field: company_ids
#             reference_value:
#               _url_query: _id
#             prefill:
#               company_ids:
#                 - _url_query: _id
