app_name:
  _ref:
    path: app_config.yaml
    key: app_name
fields:
  profile:
    _ref: modules/shared/profile/fields.yaml
request_stages:
  write: []
event_display:
  demo:
    create-profile: "{{ user.profile.name }} created their profile"
    update-profile: "{{ user.profile.name }} updated their profile"
