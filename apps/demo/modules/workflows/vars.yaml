workflows_config:
  _ref: modules/workflows/workflow_config/workflows.yaml
app_name:
  _ref:
    path: app_config.yaml
    key: app_name
entities:
  leads-collection:
    page_id: lead-view
    id_query_key: _id
    title: Lead
  # key is the module-scoped connection id that
  # _module.connectionId { id: companies-collection, module: companies } resolves to
  companies/companies-collection:
    page_id: companies/view
    id_query_key: _id
    title: Company
