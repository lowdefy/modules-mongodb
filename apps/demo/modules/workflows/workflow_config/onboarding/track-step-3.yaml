type: track-step-3
kind: tracker
action_group: g3
sort_order: 3
description: Track installation step 3.
blocked_by:
  - track-step-2
access:
  demo:
    roles:
      - admin
    verbs:
      - view
status_map:
  in-progress:
    demo:
      message: "Installation step 3 in progress."
  done:
    demo:
      message: "Installation step 3 complete."
  not-required:
    demo:
      message: "Installation step 3 skipped."
tracker:
  workflow_type: installation
