type: assign-account-manager
kind: check
action_group: setup
description: Assign an account manager to the company.
access:
  demo:
    view: true
    edit: true
status_map:
  action-required:
    demo:
      message: Assign an account manager.
  done:
    demo:
      message: Account manager assigned.
