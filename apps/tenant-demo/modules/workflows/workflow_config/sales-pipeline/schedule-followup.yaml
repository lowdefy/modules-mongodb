type: schedule-followup
kind: check
action_group: quoting
blocked_by:
  - qualify
description: Schedule a follow-up call with the deal contact within a week of qualification.
access:
  tenant-demo:
    view: true
    edit: true
status_map:
  blocked:
    tenant-demo:
      message: Schedule the follow-up call once the deal is qualified.
  action-required:
    tenant-demo:
      message: Schedule the follow-up call.
  done:
    tenant-demo:
      message: Follow-up call scheduled.
