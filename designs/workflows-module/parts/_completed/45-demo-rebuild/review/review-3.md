# Review 3

## Unowned claims

### 1. The `action_allowed` carry-over from Part 38 task 20 has no owner here — and its item pointer is circular

> **Resolved.** Change 11, the Files-changed row, and the task-20 stub now state what the carry-over concretely collapses to: notification policy → item 9; the `action_allowed` concern → the per-verb `access` config (items 1–2, which exercises the module templates' gates) plus the role-gated e2e approve (item 10). All note that no app-side `action_allowed` consumer exists — every consumer is module-owned and was migrated by Part 38's own template work. Circular "(items 9, 11)" pointers removed.

Change 11 says task 20's non-config concerns ("per-verb `action_allowed` template consumers, lifecycle notification policy") carry over to this part, and the Files changed row for `tasks/20-demo-migration.md` maps them to "(items 9, 11)". The notification half is real (item 9). The `action_allowed` half maps to **item 11 — the supersession statement itself** — and no change item or Files-changed row implements anything for it. The task-20 stub completes the loop: "Part 45 … absorbs this task's non-config concerns (per-verb `action_allowed` template consumers, notification policy — Part 45 items 9 and 11)". Both documents point at each other; neither does the work.

Checked what the work actually is: the pre-stub task 20 (git `8a9fddd`, lines 24–26) said "Demo page templates read the per-verb `_state.action_allowed: { view, edit, review, error }` … this migrates the demo's template consumers." But **no app-side file consumes `action_allowed`** — every consumer is module-owned (`modules/workflows/templates/view|edit|review|error.yaml.njk`, `pages/simple-*.yaml`, `components/action_role_check.yaml` / `evaluateVerbGate.js`), all migrated by Part 38 task 8 and the template work. A grep over `apps/demo/` (excluding build output) finds zero hits. The original task-20 bullet described demo consumers that don't exist.

**Fix:** in change 11 (and the stub), replace the vague carry-over with what it concretely collapses to: (a) the rebuilt config authors per-verb `access` maps throughout — items 1–2 — which is what makes the module templates' per-verb `action_allowed` gates exercised by real config; (b) the e2e exercises the one role-gated verb (admin-only `review` approve, item 10 / worked-example step 3). Then fix the "(items 9, 11)" pointer — as written, an implementer auditing task-20 absorption finds an item that points back at itself.

## Stale references

### 2. `action-{interaction}` should be `action-{signal}` (change 9)

> **Resolved (auto).** Change 9 now reads `action-{signal}`, matching Part 38 design.md:567 and the `interaction` → `signal` rename.

Change 9 cites "Part 38's `SubmitWorkflowAction → action-{interaction}` event-type table". The table reads `action-{signal}` (Part 38 design.md:567), and Part 38 renames `interaction` → `signal` everywhere as an explicit one-concept-one-name decision — "the legacy `metadata.interaction` key is renamed to `metadata.signal` … greenfield, no compat shim; one concept, one name" (38 design.md:578; same rename at :407 and in the hook envelope, :721). The derived type `action-approve` is correct either way, but the canonical-example design shouldn't carry the legacy key name Part 38 just retired. One-word fix.

### 3. D3's `fsm/tables.js:103-104` citation was shifted by its own resolution

> **Resolved (auto).** D3's citation updated to `fsm/tables.js:109` (verified: `none` row at 104–107, `blocked.unblock` at 109). The `hasReview` `:26` citation left as-is per the finding.

D3 cites the tracker unblock transition as "`tracker.blocked.unblock → action-required`, `fsm/tables.js:103-104`". The tracker `none` row that D3's review-1 #2 resolution added (landed via Part 38 task 23) now occupies lines 104–107; the `blocked.unblock` row sits at `fsm/tables.js:109`. The transition itself is verified present and correct — only the line numbers drifted. Update the citation (and note `hasReview` cited at `fsm/tables.js:26` in the authoring-sketches prose is the function body at :24-28 — still accurate enough to leave).

---

## Verified clean (no finding)

Checked and confirmed, for the record: all review-1/review-2 resolutions hold in the current text. Engine semantics re-verified against the landed rebuild — tracker `none` row (`activate`/`block`, tables.js:104-107), mirror signals (`internal_mirror_child_active → in-progress`, `internal_mirror_child_completed → done`, tables.js:110-111; `not-required` reachable via child-cancel, so the sketch's "Conversion skipped" cell is displayable config), `hasReview` derived from any app block declaring `review` in `access` (tables.js:24-28), `planAutoUnblock` group-target resolution (`groupById.get(entry)?.status === 'done'`, planAutoUnblock.js:83-84) and zero-docs-unsatisfied type resolution (:86-88), workflow auto-completion when all actions terminal (planWorkflowRecompute.js:92-107), `form`/`fields` distinct hook-payload keys (buildHookPayload.js:36,38), spawn-entry key set `{type, key, action_id, signal, upsert, fields, metadata}` (invokePreHook.js:8-16), post-commit `send-notification` dispatch with `{ event_ids: [event_id] }` (commitPlan.js:239-246, dispatchNotifications.js:22-27). The legal-seed rule (build + runtime) is **not yet implemented**, but its owner — Part 38 task 17, per review-2 #2's resolution — is still pending (tasks 12–19 unlanded), so the resolution holds; this part's prerequisites note already gates on it. Hook authoring grammar `hooks.{signal}.{phase}.routine:` matches both the resolver's `phases[phase]` lookup (makeWorkflowApis.js:28-44) and the old config's shape. Part 44 `start_link` grammar (`pageId`/`urlQuery`, `action_id`/`entity_id` sentinels, `edit`-verb gating) matches Part 44:7-9,38-42,94-102. Part 34 access grammar matches (`access: { demo: { view: true, edit: true } }`; verbs view/edit/review/error; demo's app slug verified as `demo` via `app_config.yaml`). `new-event` payload keys (`type`/`display`/`references`/`metadata`) match the endpoint (new-event.yaml:11-26) and the `display: { demo: { title } }` shape matches create-company's own call. Field components `text_input`/`text_area`/`yes_no_selector` (boolean values)/`file_upload` all exist; `GrDocumentText`/`AiOutline*` icons already in use. All Files-changed deletions exist (installation config, raw-insert API + its `apis:` ref, lead-view buttons + `installation_child_id` JS, `tracker-only-onboarding.spec.js`); `create-company`'s `:return:`-inside-third-segment claim, the missing `url_query` forward in `new.yaml`, the commented-out `send_routine` (vars.yaml:5-6), and the workflows `entities` var shape all match the codebase. Part 41 confirms the send_routine-decides-recipients interim model change 9 uses; Part 22 exists and owns the exhaustive-coverage split.
