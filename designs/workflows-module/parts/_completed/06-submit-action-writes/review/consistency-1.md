# Consistency Review 1

## Summary

Part-scoped sweep of part 6 (SubmitWorkflowAction) following the [review-1 action-review pass](designs/workflows-module/parts/_completed/06-submit-action-writes/review/review-1.md) (13 findings annotated) and the [top-level consistency-3 cascade](../../../review/consistency-3.md) (which propagated `CloseWorkflow` into the engine spec). Found 1 inconsistency — auto-resolved.

## Files Reviewed

- `parts/06-submit-action-writes/design.md`
- `parts/06-submit-action-writes/review/review-1.md` (13 findings; all annotated)

**Cross-part references checked (for cross-link validity, not edited):**

- `designs/workflows-module-concept/engine/spec.md` (Action `error` transition + Priority rule + SubmitWorkflowAction payload anchors)
- `designs/workflows-module-concept/submit-pipeline/spec.md` (Flow anchor)
- `designs/workflows-module-concept/action-authoring/spec.md` (`required_after_close` reference)
- `parts/05-start-cancel-handlers/design.md` (Shared internal helpers anchor)
- `parts/07-group-state-machine/design.md` (Auto-complete check + completed_groups anchors + step 5 ownership)
- `parts/09-hook-invocation/design.md` (Pre-hook return merge anchor)
- `parts/23-close-workflow-handler/design.md` (introduced during the cascade)

No tasks/ directory yet (will be generated by `/r:design-task`); no plan files.

## Decision Register (from [review-1.md](designs/workflows-module/parts/_completed/06-submit-action-writes/review/review-1.md))

| # | Topic | Resolution |
|---|---|---|
| 1 | Payload translation seam | Resolved — added Payload sub-section; per-entry-only `force` (cascaded) |
| 2 | Per-entry write loop scope | Resolved — ships in v1; only `currentActionId` slot populated; parts 7/9 plumb extras |
| 3 | `updateAction.js` location | Resolved — `src/connections/shared/`, extends part 5's scaffold in place |
| 4 | `actionsEnum` priority lookup | Resolved — `connection.actionsEnum[stage].priority`; throw on unknown stage |
| 5 | Idempotency model | Resolved — priority rule + self-exception, not the payload triple |
| 6 | Validation return shape | Resolved — dual paths; closed open question |
| 7 | `required_after_close` deferral | Resolved — terminal-workflow gate in step 1; spawned part 23 |
| 8 | `completed_groups: []` seam | Resolved — placeholder annotated, part 7 swaps |
| 9 | `form` + `form_review` merge | Resolved — one flat bag before write |
| 10 | hook-response `null` literals | Rejected — intentional field-presence commitment |
| 11 | Lifecycle numbering | Resolved — note added; follows submit-pipeline |
| 12 | Access check phrasing | Resolved — "role gate", verb filter implicit |
| 13 | `not_required` re-click | Resolved — audit-entry on self-exception is intentional |

## Inconsistencies Found

### 1. Engine-internal `force: true` caller list missing `CloseWorkflow`'s sweep

**Type:** Internal contradiction (Part 6 ↔ engine spec)
**Source of truth:** [engine/spec.md § Priority rule (line 307)](../../../workflows-module-concept/engine/spec.md) — updated during the part-6 action-review cascade and consistency-3 — names `CloseWorkflow`'s sweep alongside the `error` transition, tracker subscription, `StartWorkflow`'s parent-link push, and `CancelWorkflow`'s sweep as engine-internal force-pushes.
**Files affected:** `parts/06-submit-action-writes/design.md` — two lists:

- **Line 87** (Priority rule bullet about force callers): "(the `error` transition, tracker subscription's parent push, `StartWorkflow`'s parent-link push, `CancelWorkflow`'s sweep)" — missing `CloseWorkflow`'s sweep.
- **Line 102** (Sub-modules bullet about existing `force: true` callers of `updateAction.js`): "(`StartWorkflow`'s parent push, `CancelWorkflow`'s `not-required` loop, tracker subscription, pre-hook entries)" — missing `CloseWorkflow`'s sweep.

**Resolution:** Added `CloseWorkflow`'s sweep (with cross-link to part 23) to both lists. The caller surface in part 6's design now matches the engine spec's canonical list.

## No Issues

Everything else checked was consistent:

- **Payload section** ↔ engine spec `SubmitWorkflowAction` payload — same shape, same per-entry-only force commitment.
- **Lifecycle scaffold executing-steps list** {1, 3, 4, 5, 6} matches per-step bullets (steps 2, 7–11 all marked no-op with the right part pointers).
- **Step 1 dual validation paths** ↔ engine spec § Action `error` transition — anchor resolves to `#action-error-transition`; throw-vs-force-push split matches.
- **Step 5 + return-shape cross-link** ↔ part 7 § completed_groups return shape — part 7's design explicitly promotes step 5 from "summary only" to "summary + groups" (line 21), so part 6's "step 5's group recompute" cross-link is accurate for the post-part-7 state.
- **Step 6 form-data write** ↔ engine spec § Form data layout — both commit `form` + `form_review` merge into one flat bag with no `.review` sub-key.
- **Interaction → target-status mapping scope clarification** (applies to `currentActionId` only; auto-unblock + pre-hook entries bypass) ↔ part 9 design § Pre-hook return merge — consistent.
- **Idempotency** ↔ engine spec § Idempotency — both derive from priority rule + same-stage workflow guard, not a payload triple.
- **Sub-modules: `updateAction.js` extends part 5's scaffold in place** ↔ part 5 design.md § Shared internal helpers and shipped `plugins/modules-mongodb-plugins/src/connections/shared/updateAction.js` JSDoc ("future task in part 06 makes this optional") — consistent.
- **Out-of-scope part 23 bullet** ↔ part 23 design § Shared close/auto-complete write helper — auto-complete attributed to part 7 in both files (consistency-3 fix held).
- **Verification section** — every bullet reflects the post-cascade contract (per-entry force only, audit-entry on self-exception, form/form_review merge, terminal-workflow gate).
- **Open questions** — open question 1 closed per #6; only `current_status` provenance remains.
- **Contract to neighbours** — parts 7/8/9/10/11/13 references all match their target designs.

## Files Modified

- `parts/06-submit-action-writes/design.md` — 2 in-place edits to bring the engine-internal force caller lists in line with the canonical engine spec (added `CloseWorkflow`'s sweep).
