# Part 51 — Build list (ready now)

Mechanical/specified fixes with no open decisions. Work top-to-bottom; heavier items at
the bottom. Grouped by surface so commits stay per-package. Verify config with
`pnpm ldf:b` from `apps/demo`. See `design.md` for detail per fix.

## Block plugin (`plugins/modules-mongodb-plugins/src/blocks/...`)

- [x] **F4** — Timeline action affordance renders as a button, not a text link. `EventsTimeline.js:376-408` (`affordanceStyle` `<a>`/`<Link>`). Status-tinted button (mid-tone fill, accent text).
- [x] **F5** — Drop the informal "Go" default affordance title. `EventsTimeline.js:374` — now defaults to "View".
- [x] **F23** (D3) — `ActionSteps` pulls status colours from the shared `action_statuses.yaml` enum (new `actionStatusConfig` prop) instead of its hardcoded `actionStatusColorMap`. Removed the duplicate map; enum already has a distinct teal `in-progress`. Wired in `actions-on-entity.yaml`.

## Timeline ordering / engine (engine + module)

- [x] **F15** — Lead timeline reversed; latest belongs at top. Double-reverse bug: `GetEventsTimeline.js` `$sort:{date:-1}` + `workflows-events-timeline.yaml:93-96` `reverse:true` + `EventsTimeline.js:685`. Pick one place to reverse. **Fix:** engine `$sort:{date:-1}` stays the single source of truth; changed `workflows-events-timeline.yaml` `reverse` var default `true`→`false` so the block renders in array order (latest at top). Var kept for opt-in oldest-first.
- [x] **F12** — Timeline action order follows workflow order (currently events `date`-desc, cards by `sort_order` only within an event). **Superseded by [Part 54 — Workflow action ordering](../_completed/54-action-ordering/design.md)**, which found the same dead `sort_order` sort across all four read engines and generalized the fix (declaration-order comparator). `GetEventsTimeline.js`.
- [x] **F7** (D-resolved) — Timeline avatars resolve via the engine instead of falling to initials. Added a `$lookup` in `GetEventsTimeline.js` (after the display-field `$addFields`) on `created.user.id` → `user-contacts._id`, projecting `profile.picture` into `created.user.picture` via `$arrayElemAt` + `$ifNull` (always present, `null` when unmatched). Block reads `user.picture` at `EventsTimeline.js:86`, falls back to `getInitials` when absent. Code-time resolved: (1) join key confirmed — the existing `contact_page_url: /contacts/view?_id={id}` fills `{id}` with `user.id` and the contacts view queries `user-contacts` by `_id`, so session `user.id` === `user-contacts._id` (a user IS a contact, shared `_id` space — see user-admin CHANGELOG #31); (2) added `contactsCollection` (default `user-contacts`) to the WorkflowAPI `schema.js` and read it in the resolver — same default-only pattern as `eventsCollection` (not wired explicitly in `workflow-api.yaml`). +3 tests in `GetEventsTimeline.test.js` (resolves picture / null on no match / null on no `profile.picture`); all 28 pass.

## Module YAML (`modules/...`)

- [x] **F1 / F2 / F6** (D1) — Routed action templates + overview/group-overview through the shared title-block with `show_back_button: true`; retired `entity_back_button`. **Extended `title-block.yaml`** with three built-in optional props, wired through `layout/components/page.yaml`: `back_link` ({ pageId, urlQuery } → explicit nav, else browser-back); `badge_text` + `badge_color` (full-height status pill left of the title — caller passes text+colour only; style under `.element`). **F1 title *value* deferred to Part 53** (Titles strategy → `humanizeSlug(type)` into `page_config.title`); templates keep `title: page_config.title` with no inline fallback (action-page titles render empty until Part 53/config; back button shows regardless). Overview titles come from runtime API state and render today. `templates/{view,edit,review,error}.yaml.njk`, `workflow-overview.yaml`, `workflow-group-overview.yaml`.
- [x] **F8** — Fuller workflow-overview header (more than just "Onboarding"). Already addressed: header carries title + full-height lifecycle-stage badge + progress bar + "X of Y done" summary (the parts 17/18 intended header). Original "just Onboarding" complaint resolved; no further elaboration in design. `workflow-overview.yaml:14-130`.
- [x] **F9** — Overview action cards: add gap + group by action group. `workflow-overview.yaml:153-294`.
- [x] **F17** — Fix actions-on-entity header wrapping onto the steps. Two distinct wrapping bugs were present. **(1) Header:** title + overview button + ActionSteps were flat siblings in the List's default wrapping row, so they tangled. Set the List `direction: column` and grouped title + button into a header Box (`entity_workflows.$.header`, default row, `align: middle`) so the steps stack below the header. `components/actions-on-entity.yaml`. **(2) Action items:** within an ActionSteps group, each action `Badge` was an inline-block flowing side-by-side and wrapping (the screenshot). The per-badge `width: 100%` CSS that was meant to stack them **never loads** — see the CSS-module note below. Fixed structurally: the group description now wraps its action badges in a flex-column `<div>` (inline style, no theme tokens) so they stack regardless of CSS bundling. `ActionSteps.js`. **CSS-module bug (root cause, now FIXED):** the plugin blocks shipped `:global()` styling via `*.module.css` imported only for side effects, and **none of those rules reached the app** — the client is bundled by **Vite** (`.lowdefy/server/vite.config.js`, entry `client/main.jsx`), and Vite/Rollup tree-shakes a side-effect-only `.module.css` import because the CSS-module proxy is treated as side-effect-free (its exported class map is unused). Every selector in all four files (ActionSteps, EventsTimeline, DataDescriptions, SmartDescriptions) was already `:global(...)`, so they were CSS modules in name only. **Fix:** renamed each `style.module.css` → `style.css` and import as a plain global stylesheet (Vite always keeps plain-CSS side effects). Verified via direct `vite build`: before, 0 occurrences of `action-steps-badge`/`events-timeline-compact`/`dataview-value` in the client CSS; after, all present. Changeset `plugin-blocks-global-css-load.md`. This resurrects the intended styling for all four blocks (timeline rails, dataview value styling, etc.), not just F17.
- [x] **F20** (D4) — **Reversed decision (user):** don't render extra titles — fix the message. Rewrote the `status_map.blocked` messages to describe the action (lead with the action verb, fold in the gating condition) instead of naming only the blocker. Copy-only; no card/row/ActionSteps render change. Demo configs: `kickoff-call.yaml` ("Schedule the kickoff call once an account manager is assigned."), `send-quote.yaml`, `schedule-followup.yaml`, `track-company-setup.yaml`, `upload-po.yaml`.
- [x] **F26** (D8) — Done-action overview cards blanked ("No data to display"). Root cause was the calling YAML passing the `form_meta` *envelope* `{ form, form_review }` to `DataDescriptions`, which expects a flat items array. Fixed both cards to `formConfig: { _array.concat: [ _if_none(form_meta.form, []), _if_none(form_meta.form_review, []) ] }` (matches the reference `DataView` call). `workflow-overview.yaml:269`, `workflow-group-overview.yaml:286`. No block change. See `tasks-decisions.md` F26 for the full decision (no nested auto-render).
- [x] **F11** (D-resolved) — Qualify "Contact name" → reusable contact field. Added **two** field components wrapping the contacts module's `contact-selector` export (`_ref: { module: contacts, component: contact-selector }`), per "support single + multiple → 2 components": `components/fields/contact.yaml` (`max: 1`) and `components/fields/multiple_contact.yaml` (optional `max`). Both pass field vars (key→id, title→label.title, required, visible, validate, label_inline/span) through; the export's own required-validate clause is reused (not duplicated). Block value is an array of denormalized `{ contact_id, name, email, verified }`. Added a `contacts` dependency to the workflows manifest (auto-wired in the demo; no cycle). Demo `qualify.yaml` `form.contact_name` (text_input) → `form.contact` (contact). Build-verified the nested cross-module ref expands to a `ContactSelector` block at `form.contact` with correct request/state ids. **Timeline collection (separate ask):** kept `contactsCollection` as a workflows **module var** `contacts_collection` (default `user-contacts`) wired into `workflow-api.yaml`, with a TODO that it duplicates the contacts module's collection name (a custom contacts connection can override the collection; deriving it cross-module deferred). **e2e (not verified — needs `/r:dev-test`):** updated `onboarding-happy-path.spec.js` to drive the picker (add-new-contact flow) and comment-only refs in `form-submit-buttons.spec.js`.
- [x] **F22** (D-resolved) — Timeline cards open the check-action modal instead of navigating; handler baked into both hosts, no consumer onclick. New shared `components/check-action-click.yaml` (root = action sequence: `check` → SetState `check_action_modal.action_id` + CallMethod `setOpen`; else → Link to `action.link`), `_ref`d by default into both `actions-on-entity.yaml` (replaced inline lines 73-105) and `workflows-events-timeline.yaml`. Both hosts fire the identical `onActionClick → { action }`, so one handler serves both — verified in the compiled `lead-view.json`: EventsTimeline + ActionSteps each carry the 3-action handler. **Deleted `on_action_click`** (var + `_build.if` null-guard + doc) from the component, manifest, and README. Added **`include_modal`** (default `false`) + **`on_action_complete`** (default `[]`) on `workflows-events-timeline`: when `include_modal` true, the wrapper bundles the `check-action-modal` `_ref` (via `_build.if` over the `blocks` `_build.array.concat`) with `on_complete = [refetch get_events_timeline, ...on_action_complete]`. Default-false path verified (no duplicate modal on lead-view, still exactly 1 from `actions-on-entity`); true-path verified to bundle the modal (collides with `actions-on-entity`'s `get_workflow_action` as documented). Rule "set `include_modal: true` only on a timeline-only page with no action surface" documented in manifest + README. Demo `lead-view.yaml` keeps defaults. `pnpm ldf:b` clean. **Build-artifact note:** `pnpm ldf:b` writes `.lowdefy/server/build`, not `.lowdefy/dev/build` (that's `ldf:d` dev mode and goes stale) — inspect the server build.
- [x] **F19** (D2, partial) — check-action modal opened broken (badge showed its block id, empty status history, always `view` mode). **(0) Badge bug:** `check-action-surface.yaml` passed the status label as `content:` and the colour overrides as `properties.style:`, but the antd `Tag` block reads `properties.title` (falling back to `properties.title ?? blockId` → it rendered `action_status_badge`) and ignores `properties.style` (`additionalProperties: false`). Fixed both Tags (header + history rows): `content:` → `title:`, and moved `titleColor`/`borderColor` to block-level `style` (operators work there). **(1)** Added an Edit affordance to `check-action-surface.yaml` view mode — gated on `allowed.edit`, flips `current_action.mode` to `edit` in place (the surface renders all modes, so no page nav like the form `view.yaml.njk` `button_edit`). **(2)** Root cause of view-mode/empty-history (diagnosed live): the status-history List binds `id: current_action.status`, and `Slots.updateState` deletes the state of any non-visible block keyed by its blockId. `createSetState` runs `update()` after every SetState, and the open handler set `current_action` (with `status`) and `mode` in *separate* steps — so the update between them ran with `mode` unset → `status_history_card` (visible: mode==view) hidden → its List pruned → `current_action.status` deleted → `set_mode` then read `status.0.stage` as undefined → mode collapsed to `view` and history rendered empty. **Fix:** derive mode from the GetWorkflowAction *response* (`_request: get_workflow_action.*`, stable; `_state` would still be empty since params evaluate pre-SetState) and write it in the **same** SetState as the spread/seeds (one update cycle), `current_action` keyed first. `check-action-modal.yaml`. action-required check actions now open in `edit` (comment + Mark Started/Submit); `done` etc. open `view` with populated history + Edit button. **(2a) Header in all modes:** with the mode fix, edit-mode actions had no title/tag — the surface `action_header` was gated view/review only, but both the modal and the action pages use `hide_title` and rely on the surface for chrome. Made `action_header` (title + status Tag) unconditional. **(2b) Stable stage scalar:** the header badge (and the error-stage comment gate) read `current_action.status.0.stage`, which is pruned in every non-view mode (same List-blockId pruning as 2) — so in edit/review mode the badge stage went undefined and the Tag fell back to its block id again. Introduced a seeded scalar `current_action.stage` (`_request: get_workflow_action.status.0.stage`), bound by no block so never pruned; surface reads it for the header/comment, keeping `current_action.status[.$]` only for the view-only history List. Seeded in all four containers (modal onOpen + workflow-action-{view,edit,review} onMount). Cause **(3)** empty body deferred to Part 24 (universal-fields stub). Build-verified; needs a live re-test to confirm.

## Demo (`apps/demo`)

- [x] **F25** (D7) — Polish demo create/lifecycle event messages (verb-led, distinguishable) + lean on `event_types` icon/colour. ("Test 2 created" barebones.)
- [x] **F18** (D-resolved) — Billing-details submit updates the company doc via a `submit` **post-hook** (not pre-hook, not the dropped `entity_update`). `hooks: { submit: { post: { routine: [ MongoDBUpdateOne, :return: ] } } }` against the companies collection. **Entity id:** `_payload: context.workflow.entity_id` — the company `_id`, a **string** consecutive id (`MongoDBInsertConsecutiveId`), so the `filter._id` needs no ObjectId coercion. **Connection:** `_module.connectionId: { id: companies-collection, module: companies }` — the identical ref `company-setup.yaml`'s `entity_collection` uses; resolves to `companies/companies-collection` in the build (the routine is copied verbatim into the emitted `InternalApi` and walked in the same scope), even though `companies` isn't a declared workflows dependency (explicit cross-module `module:` resolves against app entries). **Fields:** `$set` `billing.email` / `billing.vat_number` from `_payload: form.billing_email` / `form.vat_number` (form values arrive in the payload form bag, no `form.` prefix — qualify.yaml precedent), plus `updated` via `_ref: { module: events, component: change_stamp }`. Idempotent (D6): a plain `$set` replays safely. First post-hook precedent in the repo. `apps/demo/.../company-setup/billing-details.yaml`. **Build-verified:** emitted `company-setup-billing-details-submit-post` InternalApi (`auth.public: false`) with all refs resolved; submit endpoint hooks map = `{ submit: { post: "workflows/company-setup-billing-details-submit-post" } }`. Live write not exercised (needs `/r:dev-test`).

## Heavier — forms / file upload (do last)

- [ ] **F16** (D5) — File upload broken: `upload_files` request not on page. Template must provide the S3 upload (and download) request(s) when a form has a file field, built-in. `file_upload.yaml`, `templates/edit.yaml.njk`. **Root — do first in this cluster.**
- [ ] **F13** — Quote action gets a file upload. Demo config only, after F16. `apps/demo/.../onboarding/send-quote.yaml:14-21`.
- [x] **F10** — Submit returns to the entity + shows a success prompt, built-in. `templates/edit.yaml.njk` submit/modal `onClick`/`onOk`.

## Lowdefy / theme (fix in the lowdefy repo, not this module)

Not a workflows-module bug — the workflows config plumbs `required`/`validate` through correctly. Root cause is app-wide Lowdefy/theme. Listed here so they're not lost; fix lands in the lowdefy repo / demo theme.

- [x] **F14** — Validation indicator colour is wrong.
- [x] **F21** — Required red star (asterisk) on required fields.
