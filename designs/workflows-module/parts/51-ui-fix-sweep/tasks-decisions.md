# Part 51 — Decisions list (resolve in parallel)

Each item is blocked on a decision, a live env, or Part 24. Resolve the question, then
either fold the fix into the build list or do it here. See `design.md` Open questions / D2 / D6 / D8.

- [x] **F24** (D6) — Default event messages: per-signal verbs from human titles. **Superseded by part 53 (`../53-titles/design.md`).** Resolved there as part of a module-wide titles strategy: action `title` is **optional with a `humanizeSlug`-derived default** (not required, not a naive prettify), and event messages become a curated per-signal verb map over the action/workflow title. Engine change still lands in `planEventDispatch.js:13-23`, but under part 53.
- [ ] **F26** (D8) — Overview cards' wrong "no data" states on done actions. **Decision:** should `DataDescriptions` auto-render nested objects when a `formConfig` is present (dataview-parity), or must nesting always be declared in config? `processConfigItems.js:82-83`. **Also verify separately:** is `form_meta` even non-null on the done-action card, and does stored `form_data[type]` nest under keys declared flat?
- [x] **F11** — Qualify "Contact name" → contact selector. **Decided (A):** new reusable workflows field component, NOT demo-local. The field reuses the **contacts module's `contact-selector` export** (`_ref: { module: contacts, component: contact-selector }`) — the rich picker (Atlas `$search`, inline add/edit, stores denormalized `{ contact_id, name, email, verified }`), matching the activities-module precedent (`form_activity.yaml:97-118`). Denormalized name/email avoids a join for card/timeline display. Adds a **`contacts` dependency** to the workflows module manifest (fine). New field lives in `components/fields/` (e.g. `contact.yaml`) and wraps the export, passing field vars (id, label, required) through. `apps/demo/.../onboarding/qualify.yaml:15-18`.
  - Open sub-detail (not blocking): final var pass-through surface for the wrapper field, and whether to also expose `basic-contact-selector` for read-only pick-existing cases later.
- [x] **F18** — Billing-details submit updates the company doc (hook). **Confirmed primitive:** a `submit` **post-hook** routine with `MongoDBUpdateOne` against the companies collection. Evidence:
  - `qualify.yaml`'s pre-hook `:return:` spawns **auxiliary action signals** only — not an entity write. Wrong tool.
  - The old `entity_update` payload write was **deliberately dropped** from the submit-pipeline spec; documented replacement is "apps that need to update the entity do so from a pre/post hook."
  - **Post-hook (not pre):** fires after the workflow/actions commit is durable; payload exposes `context.workflow` / `context.action` + submitted form data → read entity id + billing fields, `$set` on company doc. (`invokePostHook.js`, `buildHookPayload.js`.)
  - Schema: `hooks: { submit: { post: { routine: [ MongoDBUpdateOne, ... ] } } }` (`makeWorkflowsConfig.js` validateHooks). No repo precedent yet — billing-details is the first.
  - Authoring detail only (not a decision): exact `_payload` field paths to read off `buildHookPayload.js` at write time; include a `change_stamp` on the `$set`. `apps/demo/.../company-setup/billing-details.yaml`.
- [x] **F22** — Timeline cards navigate instead of opening the check-action modal. **Re-decided (supersedes the earlier `on_action_click`-passthrough call).** The handler is now **baked into both hosts by default — no consumer onclick, ever.** The old decision rested on a false premise: it claimed the timeline wrapper "must not couple to workflow check actions" to preserve the `workflows → events` direction. That conflates the **block** with the **wrapper**. The `EventsTimeline` *block* stays generic — it only fires `{ name: onActionClick, event: { action } }`, identical to `ActionSteps`, and the engine already puts `{ _id, kind, link, status, message }` on every card (`GetEventsTimeline.js:25`). The handler lives in `workflows-events-timeline.yaml`, which is **already a workflows-side component** (it already `_ref`s `action_statuses.yaml`), so baking the handler in costs nothing on the dependency direction. **The consumer never needs to write `on_action_click`.**

  **Design (Option A — singleton modal + one flag):**
  - **Shared handler, baked in.** Extract the canonical kind-branch handler (`check` → `SetState check_action_modal.action_id` + `CallMethod setOpen`; else → `Link` to `action.link`) into a shared **`components/check-action-click.yaml`** `_ref`, and wire it **by default** into *both* `actions-on-entity.yaml` (replacing its inline copy at lines 73-105) and `workflows-events-timeline.yaml`. Both blocks emit the same `{ action }` payload, so one handler serves both.
  - **Delete `on_action_click`.** No current consumer needs a custom handler; the passthrough var (and its load-bearing `_build.if` null-guard in the wrapper, lines 64-78) is removed from the component and the manifest. Build for the concrete case, not the escape hatch.
  - **`actions-on-entity` keeps mounting the singleton modal** (unchanged — its fixed `check_action_modal` blockId, dropped once per page). The singleton constraint is real: the modal's component-local `get_workflow_action` request id isn't scoped, so two instances = duplicate-id build error; and `CallMethod` *throws* on a missing block (`createCallMethod.js:23` reads `.methods` off `undefined`), so the open-contract is only safe when a modal is present.
  - **New `include_modal` flag on `workflows-events-timeline`** (default `false`): when `true`, the wrapper bundles the `check-action-modal` `_ref` itself — for **timeline-only pages** (no action surface). The baked handler always opens `check_action_modal`.
  - **Result:** the canonical entity page (action surface + timeline) is **zero-config** — `actions-on-entity` mounts the modal, both surfaces' baked handlers open it. A timeline-only page sets `include_modal: true` (one flag). **Residual footgun:** a timeline-only page that opens a check while neither the flag nor `actions-on-entity` provides a modal will throw on click — rare, dev-time-obvious; document the rule "set `include_modal: true` on a timeline that has no action surface" in the manifest + README.
  - **Demo fix:** `apps/demo/pages/leads/lead-view.yaml` already mounts `actions-on-entity` (line 63), so `check_action_modal` is present → the timeline's check cards (lines 109-116) open the modal with **no wiring at all**. Leave `include_modal` at its default; nothing to add on the page.
- [x] **F7** — Timeline avatar `src` not resolved → initials placeholder. **Confirmed:** `GetEventsTimeline.js` is the right and sufficient place to join the avatar.
  - Block (`EventsTimeline.js:85`) reads `user.picture` → `<img src>`; absent → initials (`getInitials(user.name)`). Block needs `user.picture` populated.
  - Avatar stored in **`user-contacts`** at `profile.picture` (same source as contacts' `get_contacts_data.yaml:29`).
  - `GetEventsTimeline.js` builds `event.created.user` with **no `$lookup`** to `user-contacts` → picture never projected. Fix: add `$lookup` on event user id → `user-contacts._id`, project `profile.picture` into `event.created.user.picture`.
  - Code-time details (not decisions): (1) confirm join key — `event.created.user.id` === `user-contacts._id`; (2) how the connection gets the `user-contacts` collection name (it currently targets the events collection).
- [x] **F19** (D2) — "Assign account manager" check action fully broken. **Decided:** fix causes 1 & 2 in THIS part (user is fixing live + testing as they go); cause 3 waits for Part 24.
  - **(1) View-surface dead-end → fix here.** Add an Edit affordance to `check-action-surface.yaml` view mode (parity with the form `view.yaml.njk` `button_edit`).
  - **(2) Modal opens in `view` despite correct data → fix here, via live env.** Static finding: the `set_mode` derivation logic (`check-action-modal.yaml:91-125`) is **correct on paper** — sequential SetState ordering is fine, the three `_if`s and `_array.includes` arg shape are well-formed. Bug is a **runtime shape mismatch**. In the live env, right after open, inspect `current_action` and verify the two exact paths the test reads: `current_action.status.0.stage` (expect `"action-required"`; confirm `status` is a newest-first array, not nested) and `current_action.allowed.edit` (expect boolean `true`; confirm `allowed` arrives at top level of the modal's `GetWorkflowAction` response, line 56-65). **Leading hypothesis:** `allowed` not present at `current_action.allowed` (or `status` ordering differs) → test falls to `else: view`. Pages can't reveal this — they write `mode` literally in onMount and never exercise the derivation; the modal is the first path that does.
  - **(3) Empty body → Part 24** (universal-fields stub). Not absorbed here (pulling a minimal renderer forward was rejected as scope creep colliding with Part 24).
- [x] **F3** — Comment/body whitespace on action template. **Confirmed: deferred to Part 24**, no interim fix in Part 51. Root cause is the universal-fields stub (`components/universal-fields/universal-fields.yaml` = `Box, visible:false`) — same as F19 cause (3). Pulling a minimal renderer forward was rejected as scope creep colliding with Part 24's design.

## Out of scope (tracked elsewhere — not this part)

- [ ] **F14 / F21** — Validation indicator colour + required red star. App-wide Lowdefy/theme bug; workflows plumbing passes `required`/`validate` through correctly. Track against the lowdefy repo / demo theme.
