

## Authoring

Access section mixing apps actions and role is a little weird, can we do better?


responsibility was important for reporting - PRP team could do action, but was the clients responsibility.  Should allocate outstanding action to client in reports - don't think we should drop it


I want to talk about how paired trigger + tracker actions work - maybe an example?

## Engine

1. Do we need entity type and collection?


2. Lets chat about errors


3. What does "**`SubmitWorkflowAction`** writes one or more action transitions per call. " mean?
   
   Does SubmitWorkflowAction work as both the base API, as well as for other consumers to "update a specific actions status"?


## Module surface

We might have an issue because we dynamically generate module pages based on the config - exports section doesn't match generated. Will need lowdefy enhancement

One single workflow overview or one per workflow?
