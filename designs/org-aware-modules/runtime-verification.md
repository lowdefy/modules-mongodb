# Org-Aware Modules — Manual Runtime Verification

Folded into the one QA document:
[auth-tenancy-verification/qa-test-plan.md](../auth-tenancy-verification/qa-test-plan.md).
Sections 1–6 there are the browser pass; **Section 7 — Developer checks** carries
everything this file used to hold (the audit negative check, the preflight
refusal, the signup mint's on-disk assertions, search-under-the-authored-wall,
and the pinned smoke), along with the list of what the automated suites already
prove and never needs hand-testing.

One person runs both halves, so one file holds both — split by section, not by
document.
