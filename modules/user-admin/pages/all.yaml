# STUB — real body (Members + Invitations tabs, filters, export) is authored by
# the mock-to-lowdefy pipeline tasks 6–9. This resolves so the module builds.
_ref:
  module: layout
  component: page
  vars:
    id: all
    title:
      _build.string.trim:
        _build.string.concat:
          - _module.var: app_title
          - " User Admin"
    breadcrumbs:
      - home: true
        icon: AiOutlineHome
      - label:
          _build.string.trim:
            _build.string.concat:
              - _module.var: app_title
              - " User Admin"
    blocks:
      - id: all_stub
        type: Title
        properties:
          level: 4
          content: "Members + Invitations — built by pipeline tasks 6–9."
