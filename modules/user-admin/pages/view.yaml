# STUB — real body (Profile / Attributes / Global attributes / Security / Apps /
# Activity tiles) is authored by the mock-to-lowdefy pipeline tasks 10–13.
_ref:
  module: layout
  component: page
  vars:
    id: view
    page_type: sidebar
    title:
      _build.string.trim:
        _build.string.concat:
          - _module.var: app_title
          - " User"
    breadcrumbs:
      - home: true
        icon: AiOutlineHome
      - label:
          _build.string.trim:
            _build.string.concat:
              - _module.var: app_title
              - " User Admin"
        pageId:
          _module.pageId: all
      - label: User
    blocks:
      - id: view_stub
        type: Title
        properties:
          level: 4
          content: "User detail — built by pipeline tasks 10–13."
