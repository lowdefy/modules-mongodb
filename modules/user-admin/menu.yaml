id: default
links:
  - id: users
    type: MenuLink
    pageId:
      _module.pageId: all
    properties:
      title:
        _build.string.trim:
          _build.string.concat:
            - _module.var: app_title
            - " User Admin"
      icon: AiOutlineTeam
