# Who and when, inline because this module declares no dependencies. Same shape as the events
# module's stamp, but an app's central additions to that stamp do not reach this one.
timestamp:
  _date: now
user:
  name:
    _user: profile.name
  id:
    _user: id
