# Read-only by construction: AnalyticsQuery is this connection's only request
# and its compiler emits only read stages. Datasets choose their collection per
# request, so this connection is not remappable — the app supplies the URI
# secret instead.
id: reporting-data
type: ReportingData
properties:
  databaseUri:
    _secret: REPORTING_DATA_MONGODB_URI
