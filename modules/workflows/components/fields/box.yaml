vars:
  key:
    type: string
    required: true
  visible:
    type: boolean
    default: true
  blocks:
    type: array
    default: []

config:
  id:
    _var: key
  type: Box
  layout:
    contentGutter: 16
    contentJustify: center
  visible:
    _var:
      key: visible
      default: true
  blocks:
    _var:
      key: blocks
      default: []
