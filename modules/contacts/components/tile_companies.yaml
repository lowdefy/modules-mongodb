_ref:
  module: layout
  component: card
  vars:
    title: Companies
    blocks:
      - id: contact_companies_list
        type: Box
        requests:
          - _ref: requests/get_contact_companies.yaml
        events:
          onMount:
            - id: fetch_companies
              type: Request
              params: get_contact_companies
        blocks:
          - _ref: components/company_list_items.yaml
