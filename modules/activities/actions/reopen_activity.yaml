- id: reopen
  type: CallAPI
  params:
    endpointId:
      _module.endpointId: change-activity-status
    payload:
      activity_id:
        _payload: activity_id
      stage: open
  messages:
    success:
      _string.concat:
        - _module.var: label
        - " reopened."
