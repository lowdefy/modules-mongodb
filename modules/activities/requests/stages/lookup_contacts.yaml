$lookup:
  from: user-contacts
  localField: contact_ids
  foreignField: _id
  as: contacts
  pipeline:
    - $match:
        deleted.timestamp:
          $exists: false
    - $project:
        _id: 1
        profile: 1
        email: 1
        lowercase_email: 1
