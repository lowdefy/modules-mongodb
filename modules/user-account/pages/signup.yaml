# Stub — built out in task 10. Minimal valid auth page so `_ref`s resolve and
# the module builds; real content added by the screen task.
_ref:
  module: layout
  component: auth-page
  vars:
    id: signup
    title: Sign up
    blocks:
      - id: placeholder
        type: Paragraph
        properties:
          content: TODO(screen) — Sign up page, built in task 10.
