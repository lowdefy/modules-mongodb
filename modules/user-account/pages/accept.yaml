# Stub — built out in task 15. Minimal valid auth page so `_ref`s resolve and
# the module builds; real content added by the screen task.
_ref:
  module: layout
  component: auth-page
  vars:
    id: accept
    title: Accept invitation
    blocks:
      - id: placeholder
        type: Paragraph
        properties:
          content: TODO(screen) — Accept invitation page, built in task 15.
