# Stub — built out in task 18. Minimal valid protected page so `_ref`s resolve
# and the module builds; real content added by the screen task.
_ref:
  module: layout
  component: page
  vars:
    id: view
    title: Account
    blocks:
      - id: placeholder
        type: Paragraph
        properties:
          content: TODO(screen) — Account page, built in task 18.
