# Stub — built out in task 09. Minimal valid auth page so `_ref`s resolve and
# the module builds; real content added by the screen task.
_ref:
  module: layout
  component: auth-page
  vars:
    id: login
    title: Login
    blocks:
      - id: placeholder
        type: Paragraph
        properties:
          content: TODO(screen) — Login page, built in task 09.
