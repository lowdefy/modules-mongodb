# Stub — built out in task 11. Minimal valid auth page so `_ref`s resolve and
# the module builds; real content added by the screen task.
_ref:
  module: layout
  component: auth-page
  vars:
    id: forgot-password
    title: Forgot password
    blocks:
      - id: placeholder
        type: Paragraph
        properties:
          content: TODO(screen) — Forgot password page, built in task 11.
