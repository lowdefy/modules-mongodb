# Stub — built out in task 12. Minimal valid auth page so `_ref`s resolve and
# the module builds; real content added by the screen task.
_ref:
  module: layout
  component: auth-page
  vars:
    id: reset-password
    title: Reset password
    blocks:
      - id: placeholder
        type: Paragraph
        properties:
          content: TODO(screen) — Reset password page, built in task 12.
