# Stub — built out in task 14. Minimal valid auth page so `_ref`s resolve and
# the module builds; real content added by the screen task.
_ref:
  module: layout
  component: auth-page
  vars:
    id: two-factor
    title: Two-factor authentication
    blocks:
      - id: placeholder
        type: Paragraph
        properties:
          content: TODO(screen) — Two-factor authentication page, built in task 14.
