# Stub — built out in task 16. Minimal valid auth page so `_ref`s resolve and
# the module builds; real content added by the screen task.
_ref:
  module: layout
  component: auth-page
  vars:
    id: logout
    title: Sign out
    blocks:
      - id: placeholder
        type: Paragraph
        properties:
          content: TODO(screen) — Sign out page, built in task 16.
