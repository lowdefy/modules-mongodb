_ref:
  module: layout
  component: card
  vars:
    id: tile_events
    title: History
    blocks:
      - _ref:
          module: events
          component: events-timeline
          vars:
            reference_field: company_ids
            reference_value:
              _url_query: _id
